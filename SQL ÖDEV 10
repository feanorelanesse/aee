SELECT city, country FROM city LEFT JOIN country ON city.country_id = country.country_id;
SELECT rental_id, first_name, last_name FROM customer RIGHT JOIN rental ON customer.customer_id = rental.customer_id;
SELECT rental_id, first_name, last_name FROM customer FULL JOIN rental ON customer.customer_id = rental.customer_id;
